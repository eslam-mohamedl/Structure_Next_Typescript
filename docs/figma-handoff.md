# Figma → Dev Handoff

- Inspect layout → get spacing, color, radius.
- Follow naming mapping: F_Button → atoms/Button.
- Extract design tokens manually before implementation.
- Export icons as SVG and optimize before using.
